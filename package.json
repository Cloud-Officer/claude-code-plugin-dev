{
  "name": "claude-code-plugin-dev",
  "version": "1.0.0",
  "private": true,
  "description": "Marker manifest: the repo ships no runtime dependencies. Its presence makes github-build emit the js_unit_tests job.",
  "license": "MIT",
  "scripts": {
    "test": "node --test 'tests/*.test.js'"
  }
}
